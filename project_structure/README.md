# Project Name

One or two sentences explaining what this project does and who it is for.

## Quick Start

```bash
# Replace this section with the actual way end users install or run the project.
# Examples:
# pip install your-project
# uv tool install your-project
```

## Development

```bash
uv sync
uv run pytest
uv run ruff check .
uv run ruff format .
uv run ty check
```

## Notes

- The top of this README should stay public-facing when copied into the real repo.
- Replace `Quick Start` with real installation or usage instructions for end users.
- Replace `src/your_project/` with the real package name.
- Update `tests/test_smoke.py` after renaming the package.
- Edit `AGENTS.md` so the instructions match the real repo.
- Use type hints in new and modified Python code.
- Keep `docs/plans/` and `docs/research/` tidy enough to reuse later.
